#!/usr/bin/env node
/**
 * @deprecated Use scripts/hash-download-admin-code.mjs instead.
 * Download Center admin auth now uses DOWNLOAD_ADMIN_CODE_HASH (admin code), not email/password.
 */
console.error('Deprecated: use node scripts/hash-download-admin-code.mjs');
process.exit(1);
